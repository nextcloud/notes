default:
  suites:
    default:
      paths:
        - %paths.base%/features/
      contexts:
        - FeatureContext:
            baseUrl: http://localhost:8080/
            admin:
              - admin
              - admin
